//const bool operator += (void);
const var & operator += (const var &);
const var & operator += (const nr &);
//const var & operator += (const char *);

//const var operator += (const text &);
const var & operator += (const std::string &);
const var & operator += (const char []);
//const var operator += (const signed char []);
//const var operator += (const unsigned char []);
const var & operator += (const char &);
//const var operator += (const signed char &);
//const var operator += (const unsigned char &);
//const var & operator += (const var &);
const var & operator += (const complex &);
const var & operator += (const real &);
const var & operator += (const rational &);
const var & operator += (const integer &);
const var & operator += (const _float &);
const var & operator += (const short_float &);
const var & operator += (const single_float &);
const var & operator += (const double_float &);
const var & operator += (const long_float &);
const var & operator += (const long double &);
const var & operator += (const double &);
const var & operator += (const float &);
const var & operator += (const signed long long int &);
const var & operator += (const unsigned long long int &);
const var & operator += (const signed long int &);
const var & operator += (const unsigned long int &);
const var & operator += (const signed int &);
const var & operator += (const unsigned int &);
const var & operator += (const signed short int &);
const var & operator += (const unsigned short int &);
