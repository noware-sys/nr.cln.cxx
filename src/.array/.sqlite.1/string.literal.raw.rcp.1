#.PHONY: clean
#.PHONY : all, clean

#out=sequence.hdr.cxx
input=('sequence.sql');
output=('sequence.hdr.cxx');
translator=./string.literal.raw.sh;

#all:
all : #$(output)
#$(OUT) : sqlite.obj #$(IN)
#$(out) :
	$(foreach element, $(output), $(eval $(translator) "$(element)" "${output[$i]}" '***'));\
clean:
	rm --force "{}";
