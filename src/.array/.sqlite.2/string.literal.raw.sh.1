#! /bin/bash


delimiter_default='';


if test $# -lt 2
then
	exit 1;
fi

if test -z "${3}"
then
	delimiter="${delimiter_default}";
else
	delimiter="${3}";
fi


> "${2}";

echo -e -n "u8R\"""${delimiter}""(\n" >> "${2}";

cat "${1}" >> "${2}";

echo -e -n ")""${delimiter}""\"\n" >> "${2}";
