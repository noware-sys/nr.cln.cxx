#pragma once

#include <string>
#include <ofstream>

namespace noware
{
	//namespace misc
	//{
			const bool log (const std::string &/* file_name*/, const std::string &/* value*/);
			const bool log (const std::ofstream &/* file*/, const std::string &/* value*/);
	//}
}
