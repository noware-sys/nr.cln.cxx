//const bool operator += (void);
const number operator += (const number &);
//const number operator += (const text &);
const number operator += (const std::string &);
const number operator += (const char []);
//const number operator += (const signed char []);
//const number operator += (const unsigned char []);
const number operator += (const char &);
//const number operator += (const signed char &);
//const number operator += (const unsigned char &);
const number operator += (const cln::cl_N &);
const number operator += (const cln::cl_RA &);
const number operator += (const cln::cl_I &);
const number operator += (const cln::cl_SF &);
const number operator += (const cln::cl_FF &);
const number operator += (const cln::cl_DF &);
const number operator += (const cln::cl_LF &);
const number operator += (const long double &);
const number operator += (const double &);
const number operator += (const float &);
const number operator += (const signed long long int &);
const number operator += (const unsigned long long int &);
const number operator += (const signed long int &);
const number operator += (const unsigned long int &);
const number operator += (const signed int &);
const number operator += (const unsigned int &);
const number operator += (const signed short int &);
const number operator += (const unsigned short int &);

